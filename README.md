# driftnet-schema-schema
痛点：生产数据管道中，Schema 假设散落在代码各处——pandas 的 df['user_id']、SQL 的 SELECT email FROM users、JSON 的 data['address']['zip']。当上游悄悄改了列名、换了类型、新增了 NULL，你的管道要么凌晨3点炸掉，要么更可怕地——静默产出错误数据。Great Expectations 等工具�...
